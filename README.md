# TaskFlow

A full-stack task management application (Flask + PostgreSQL + Bootstrap/Chart.js)
deployed via Jenkins -> Docker Hub -> Kubernetes (kind), provisioned with Ansible.

See the accompanying project guide document for full setup instructions.
